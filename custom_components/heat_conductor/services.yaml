set_vacation:
  fields:
    start:
      selector:
        datetime:
    end:
      required: true
      selector:
        datetime:
    temperature:
      selector:
        number:
          min: 5
          max: 30
          step: 0.5
          unit_of_measurement: "°C"

clear_vacation:

reset_learning:
  fields:
    room:
      selector:
        text:

boost:
  target:
    entity:
      integration: heat_conductor
      domain: climate
  fields:
    duration:
      selector:
        duration:
    temperature:
      selector:
        number:
          min: 5
          max: 30
          step: 0.5
          unit_of_measurement: "°C"

clear_override:
  target:
    entity:
      integration: heat_conductor
      domain: climate
